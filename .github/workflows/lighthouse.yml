name: Lighthouse

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Lighthouse Audit
      uses: jakejarvis/lighthouse-action@master
      with:
        url: 'https://www.chez-nicole.fr'
        netlify_site: 'chez-nicole.netlify.com'
    - uses: actions/upload-artifact@master
      with:
        name: report
        path: './report'
